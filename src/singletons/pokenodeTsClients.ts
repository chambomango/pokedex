import { PokemonClient, MoveClient, EvolutionClient } from "pokenode-ts";

export const pokemonClient = new PokemonClient();
export const moveClient = new MoveClient();
export const evolutionClient = new EvolutionClient();
